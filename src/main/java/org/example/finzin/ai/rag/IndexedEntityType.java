package org.example.finzin.ai.rag;

/** The set of user-owned entity types kept in the semantic search index. */
public enum IndexedEntityType {
    TRANSACTION,
    NOTE,
    TODO,
    ACCOUNT,
    GOLD_ASSET,
    BUDGET_PLAN,
    AI_CONVERSATION
}
