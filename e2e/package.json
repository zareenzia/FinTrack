{
  "name": "fintrack-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright end-to-end UI test suite for TakaFlow — self-contained, lives entirely under e2e/, no overlap with src/.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "typescript": "^5.6.0"
  }
}
